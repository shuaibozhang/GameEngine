#version 400 core

in vec4 textureCoords;
out vec4 out_Color;

uniform sampler2D texture;

void main(void) {
    vec2 temp = vec2(textureCoords.x/textureCoords.w * 0.5 + 0.5,textureCoords.y/textureCoords.w * 0.5 + 0.5);
	out_Color = vec4(texture(texture, temp).xyz, 0.8);

}